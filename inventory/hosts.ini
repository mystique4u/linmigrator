[fedora_target]
# For local execution on Fedora (recommended when running Ansible on the target itself)
localhost ansible_connection=local

# For remote execution (if running from another machine)
# your-fedora-host ansible_host=<IP_ADDRESS> ansible_user=<USERNAME>

[fedora_target:vars]
ansible_python_interpreter=/usr/bin/python3
